apiVersion: apps/v1
kind: Deployment
metadata:
  name: galaga-frontend
  namespace: galaga
spec:
  replicas: 2
  selector:
    matchLabels:
      app: galaga-frontend
  template:
    metadata:
      labels:
        app: galaga-frontend
    spec:
      containers:
        - name: frontend
          image: galaga-frontend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: galaga-frontend
  namespace: galaga
spec:
  selector:
    app: galaga-frontend
  ports:
    - port: 80
      targetPort: 80
